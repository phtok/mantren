---
import type { Mantra } from '../lib/mantren';

interface Props {
  mantra: Mantra;
}

const { mantra } = Astro.props;

function renderLine(line: string) {
  if (line.startsWith('/')) {
    return { text: line.slice(1), cont: true };
  }
  return { text: line, cont: false };
}
---
<article class="mantra">

  <div class="mantra-body">
    {mantra.parts.map((part) => (
      <section class="part">
        {part.regie && <p class="regie">{part.regie}</p>}
        {part.section && <h2 class="section-head">{part.section}</h2>}
        {part.lines && part.lines.length > 0 && (
          <p class="lines">
            {part.lines.map((raw) => {
              const { text, cont } = renderLine(raw);
              return (
                <span class={`line${cont ? ' line-cont' : ''}`}>{text}</span>
              );
            })}
          </p>
        )}
      </section>
    ))}
  </div>
</article>
